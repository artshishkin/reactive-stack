apply plugin: "war"

bootWar {
    mainClass = "net.shyshkin.study.store.StoreApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
